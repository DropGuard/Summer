package com.example;

import com.github.dropguard.summer.core.Component;

/** Deleted by the IT driver between build 1 and build 2. The reconciler must remove its .class
 *  from target/classes; otherwise the generated AOT context references a phantom bean. */
@Component
public class RemovableService {

    public String tag() {
        return "removable";
    }
}
