# k6 benchmark exports generated by run-benchmarks.py (--summary-export).
# Raw per-run data, overwritten on each benchmark execution; the hand-written
# analysis lives in ../benchmark-results.md and stays tracked.
summary-*.json
