package summer.tck.di.replaces.conditional;

import summer.core.annotation.Configuration;

/**
 * Entry point for conditional @Replaces TCK tests.
 * Scans the conditional replaces package only (no ReplacementComponent).
 */
@Configuration
public class ConditionalReplacesTestConfig {
}
