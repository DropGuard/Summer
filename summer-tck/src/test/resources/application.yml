app:
  name: summer-tck
  port: 8080
  verbose: true

server:
  tls:
    enabled: true
    cert-chain: /path/to/cert.pem

tls:
  enabled: true
  cert-chain: /path/to/cert.pem
  private-key: /path/to/key.pem
root:
  host: localhost
  port: 9090

web:
  host: web.local
  router-type: LINEAR
  allowed-origins:
    - https://a.example.com
    - https://b.example.com
  max-conn: 250
