<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>summer</groupId>
    <artifactId>summer-starter-parent</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>

    <name>Summer Starter Parent</name>
    <description>Parent POM for Summer applications — pre-configures AOT plugin, Jandex indexing, and compiler settings</description>

    <properties>
        <java.version>26</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--
          The version of the Summer framework this parent is paired with. It must
          match the version of the published summer-* artifacts (summer-maven-plugin,
          summer-core, ...). Kept as an explicit property (rather than
          ${project.version}) so the AOT plugin resolves to the framework version
          even when an application inheriting this parent declares a different
          project.version of its own.
        -->
        <summer.framework.version>0.1.0</summer.framework.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>summer</groupId>
                <artifactId>summer-dependencies</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.15.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <proc>full</proc>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>io.smallrye</groupId>
                    <artifactId>jandex-maven-plugin</artifactId>
                    <version>3.5.3</version>
                    <executions>
                        <execution>
                            <id>make-index</id>
                            <phase>compile</phase>
                            <goals>
                                <goal>jandex</goal>
                            </goals>
                            <configuration>
                                <classesDir>${project.build.outputDirectory}</classesDir>
                                <indexDir>${project.build.outputDirectory}/META-INF</indexDir>
                                <indexName>jandex.idx</indexName>
                                <processDefaultFileSet>true</processDefaultFileSet>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.5</version>
                    <configuration>
                        <argLine>-Xmx2048m --sun-misc-unsafe-memory-access=allow</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>summer</groupId>
                    <artifactId>summer-maven-plugin</artifactId>
                    <version>${summer.framework.version}</version>
                    <executions>
                        <execution>
                            <id>generate-aot</id>
                            <phase>process-classes</phase>
                            <goals>
                                <goal>generate-aot</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!--
                  Spotless is NOT auto-bound here: applications should not be forced
                  through a formatting gate. It is declared in pluginManagement only
                  so an external app/demo can opt in by adding the plugin to its own
                  <build><plugins> (the framework's internal build keeps its own
                  spotless binding via summer-framework).
                -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>2.44.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>summer</groupId>
                <artifactId>summer-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generate-aot</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>generate-aot</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
