server:
  port: 8080

datasource:
  url: jdbc:postgresql://localhost:5432/summer
  username: summer
  password: summer
  driver-class-name: org.postgresql.Driver

redis:
  host: localhost
  port: 6379

jwt:
  secret: a-very-secure-jwt-secret-key-that-is-long-enough-to-be-secure

cors:
  allowed-origins: "http://localhost:5173"
  allowed-methods: "GET, POST, PUT, DELETE, OPTIONS"
  allowed-headers: "Content-Type, Authorization"
  max-age: 3600
