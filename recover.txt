Created At: 2026-06-17T15:21:00Z
Completed At: 2026-06-17T15:21:00Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -175,6 +175,35 @@
 				}
 			}
 
+			// Step 3: Extract metadata and resolve conditions BEFORE instantiation
+			List<summer.core.BeanCondition> descriptors = new ArrayList<>();
+			for (Class<?> clazz : componentClasses) {
+				descriptors.add(buildCondition(clazz));
+			}
+			for (Class<?> clazz : configPropsClasses) {
+				descriptors.add(buildCondition(clazz));
+			}
+			for (String configName : configClassNames) {
+				try {
+					Class<?> configClass = Class.forName(configName);
+					for (Method m : configClass.getDeclaredMethods()) {
+						if (m.isAnnotationPresent(summer.core.annotation.Bean.class)) {
+							descriptors.add(buildCondition(m));
+						}
+					}
+				} catch (ClassNotFoundException e) {
+					log.debug("[Summer] Skipping config {} — class not found", configName);
+				}
+			}
+
+			summer.core.BeanDescriptorResolver.Result result = summer.core.BeanDescriptorResolver.resolve(descriptors);
+			Set<String> toRemove = result.typesToRemove();
+
+			// Filter out removed classes
+			componentClasses.removeIf(c -> toRemove.contains(c.getName()));
+			configPropsClasses.removeIf(c -> toRemove.contains(c.getName()));
+			configClassNames.removeIf(toRemove::contains);
+
 			for (Class<?> clazz : configPropsClasses) {
 				ConfigurationProperties ann = clazz.getAnnotation(ConfigurationProperties.class);
 				String prefix = ann != null ? ann.prefix() : "";
@@ -212,32 +212,8 @@
 				}
 			}
 
-			Map<Class<?>, Object> resolvedConfigs = new LinkedHashMap<>(configInstances);
-
-			// Handle class-level @Replaces on @Configuration classes
-			Set<Class<?>> replacedConfigs = new HashSet<>();
-			for (var entry : resolvedConfigs.entrySet()) {
-			
<truncated 1172 bytes>
ava.util.Arrays.stream(c.getClass().getDeclaredMethods())
-									.filter(m -> m.isAnnotationPresent(summer.core.annotation.Bean.class)).count())
+									.filter(m -> m.isAnnotationPresent(summer.core.annotation.Bean.class) && !toRemove.contains(m.getReturnType().getName())).count())
 							.sum());
 		}
 
+		private summer.core.BeanCondition buildCondition(Class<?> clazz) {
+			summer.core.annotation.ConditionalOnBean cond = clazz.getAnnotation(summer.core.annotation.ConditionalOnBean.class);
+			summer.core.annotation.Replaces repl = clazz.getAnnotation(summer.core.annotation.Replaces.class);
+			String condVal = cond != null ? cond.value().getName() : null;
+			String replVal = repl != null ? repl.value().getName() : null;
+			Set<String> ifaces = new HashSet<>();
+			for (Class<?> iface : clazz.getInterfaces()) {
+				ifaces.add(iface.getName());
+			}
+			return summer.core.BeanCondition.of(clazz.getName(), ifaces, condVal, replVal);
+		}
+
+		private summer.core.BeanCondition buildCondition(Method m) {
+			summer.core.annotation.ConditionalOnBean cond = m.getAnnotation(summer.core.annotation.ConditionalOnBean.class);
+			summer.core.annotation.Replaces repl = m.getAnnotation(summer.core.annotation.Replaces.class);
+			String condVal = cond != null ? cond.value().getName() : null;
+			String replVal = repl != null ? repl.value().getName() : null;
+			Set<String> ifaces = new HashSet<>();
+			for (Class<?> iface : m.getReturnType().getInterfaces()) {
+				ifaces.add(iface.getName());
+			}
+			return summer.core.BeanCondition.of(m.getReturnType().getName(), ifaces, condVal, replVal);
+		}
+
 		private boolean constructorDepsSatisfied(Class<?> clazz, BeanRegistry registry) {
 			for (Constructor<?> ctor : clazz.getConstructors()) {
 				boolean allSatisfied = true;
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T15:21:06Z
Completed At: 2026-06-17T15:21:06Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -105,8 +105,6 @@
 
 			validateRegistry(registry);
 
-			RuntimeConditionResolver.resolve(registry, index);
-
 			createAopProxies(registry);
 
 			return BeanContainer.create(registry);
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:54:48Z
Completed At: 2026-06-17T16:54:48Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -175,6 +175,13 @@
 
 			// Step 3: Extract metadata and resolve conditions BEFORE instantiation
 			List<summer.core.BeanCondition> descriptors = new ArrayList<>();
+			for (Object bean : registry.singletons().values()) {
+				Set<String> ifaces = new HashSet<>();
+				for (Class<?> iface : bean.getClass().getInterfaces()) {
+					ifaces.add(iface.getName());
+				}
+				descriptors.add(summer.core.BeanCondition.of(bean.getClass().getName(), ifaces, null, null));
+			}
 			for (Class<?> clazz : componentClasses) {
 				descriptors.add(buildCondition(clazz));
 			}
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:54:56Z
Completed At: 2026-06-17T16:54:56Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -182,6 +182,9 @@
 				}
 				descriptors.add(summer.core.BeanCondition.of(bean.getClass().getName(), ifaces, null, null));
 			}
+			for (Class<?> clazz : extraComponents) {
+				descriptors.add(buildCondition(clazz));
+			}
 			for (Class<?> clazz : componentClasses) {
 				descriptors.add(buildCondition(clazz));
 			}
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:56:38Z
Completed At: 2026-06-17T16:56:38Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -445,6 +445,8 @@
 					continue;
 				if (beanClass.isAnnotationPresent(ConfigurationProperties.class))
 					continue;
+				if (!isComponent(beanClass) && !beanClass.isAnnotationPresent(Configuration.class))
+					continue;
 
 				Set<Class<?>> deps = new HashSet<>();
 				for (Constructor<?> ctor : beanClass.getConstructors()) {
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:57:37Z
Completed At: 2026-06-17T16:57:37Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -445,9 +445,11 @@
 					continue;
 				if (beanClass.isAnnotationPresent(ConfigurationProperties.class))
 					continue;
-				if (!isComponent(beanClass) && !beanClass.isAnnotationPresent(Configuration.class))
-					continue;
-
+				if (!isComponent(beanClass) && !beanClass.isAnnotationPresent(Configuration.class)) {
+					System.out.println("DEBUG SKIPPING " + beanClass.getName() + " because it is not a component/config");
+					continue;
+				}
+				System.out.println("DEBUG VALIDATING " + beanClass.getName() + " as entry " + entry.getKey().getName());
 				Set<Class<?>> deps = new HashSet<>();
 				for (Constructor<?> ctor : beanClass.getConstructors()) {
 					for (Class<?> paramType : ctor.getParameterTypes()) {
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:58:24Z
Completed At: 2026-06-17T16:58:32Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -74,6 +74,7 @@
 	public static final class Builder {
 
 		private final List<Class<?>> extraComponents = new ArrayList<>();
+		private boolean disableJandex = false;
 
 		/**
 		 * Register an additional component class. The class must be annotated with
@@ -80,7 +80,12 @@
 		 * test-specific beans not in the Jandex index.
 		 */
-		public Builder registerComponent(Class<?> clazz) {
-			extraComponents.add(clazz);
+		public Builder registerComponent(Class<?> componentClass) {
+			extraComponents.add(componentClass);
+			return this;
+		}
+
+		public Builder disableJandex() {
+			this.disableJandex = true;
 			return this;
 		}
 
@@ -88,7 +88,7 @@
 		 * registering any extra components.
 		 */
 		public BeanContainer build() {
-			IndexView index = JandexIndexLoader.buildIndex();
+			IndexView index = disableJandex ? null : JandexIndexLoader.buildIndex();
 			BeanRegistry registry = new BeanRegistry();
 
 			registry.registerSingleton(RuntimeDiMarker.class, new RuntimeDiMarker());
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.
==================
Created At: 2026-06-17T16:58:47Z
Completed At: 2026-06-17T16:58:47Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -124,27 +124,30 @@
 
 			// Step 1: Find all annotation types meta-annotated with @Component
 			Set<DotName> componentAnnotations = new HashSet<>();
-			componentAnnotations.add(COMPONENT);
-			for (AnnotationInstance ann : index.getAnnotations(COMPONENT)) {
-				if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
-					ClassInfo target = ann.target().asClass();
-					if (target.isAnnotation()) {
-						componentAnnotations.add(target.name());
-					}
-				}
-			}
-
-			// Step 2: Find all classes annotated with any component annotation
 			Set<String> componentClassNames = new HashSet<>();
 			Set<String> configClassNames = new HashSet<>();
-			for (DotName annName : componentAnnotations) {
-				for (AnnotationInstance ann : index.getAnnotations(annName)) {
+			Set<String> configPropsClassNames = new HashSet<>();
+
+			if (index != null) {
+				componentAnnotations.add(COMPONENT);
+				for (AnnotationInstance ann : index.getAnnotations(COMPONENT)) {
 					if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
-						String name = ann.target().asClass().name().toString();
-						componentClassNames.add(name);
-					}
-				}
-			}
+						ClassInfo target = ann.target().asClass();
+						if (target.isAnnotation()) {
+							componentAnnotations.add(target.name());
+						}
+					}
+				}
+
+				// Step 2: Find all classes annotated with any component annotation
+				for (DotName annName : componentAnnotations) {
+					for (AnnotationInstance ann : index.getAnnotations(annName)) {
+						if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
+							String name = ann.target().asClass().name().toString();
+							componentClassNames.add(name);
+						}
+					}
+				}
 			for (AnnotationInstance ann : index.getAnnotations(CONFIGURATION)) {
 				if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
 					configClassNames.add(ann.target().asClass().name().toString());
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:58:53Z
Completed At: 2026-06-17T16:58:53Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -148,21 +148,21 @@
 						}
 					}
 				}
-			for (AnnotationInstance ann : index.getAnnotations(CONFIGURATION)) {
-				if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
-					configClassNames.add(ann.target().asClass().name().toString());
-				}
-			}
-			Set<String> configPropsClassNames = new HashSet<>();
-			for (AnnotationInstance ann : index.getAnnotations(CONFIGURATION_PROPERTIES)) {
-				if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
-					String name = ann.target().asClass().name().toString();
-					configPropsClassNames.add(name);
-					componentClassNames.add(name);
-				}
-			}
-			log.info("[Summer] Found {} @Component-annotated annotation types, {} candidate classes, {} @Configuration, {} @ConfigurationProperties",
-					componentAnnotations.size(), componentClassNames.size(), configClassNames.size(), configPropsClassNames.size());
+				for (AnnotationInstance ann : index.getAnnotations(CONFIGURATION)) {
+					if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
+						configClassNames.add(ann.target().asClass().name().toString());
+					}
+				}
+				for (AnnotationInstance ann : index.getAnnotations(CONFIGURATION_PROPERTIES)) {
+					if (ann.target().kind() == AnnotationTarget.Kind.CLASS) {
+						String name = ann.target().asClass().name().toString();
+						configPropsClassNames.add(name);
+						componentClassNames.add(name);
+					}
+				}
+				log.info("[Summer] Found {} @Component-annotated annotation types, {} candidate classes, {} @Configuration, {} @ConfigurationProperties",
+						componentAnnotations.size(), componentClassNames.size(), configClassNames.size(), configPropsClassNames.size());
+			}
 
 			for (String className : componentClassNames) {
 				if (className.contains("$"))
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:58:58Z
Completed At: 2026-06-17T16:58:58Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -193,9 +193,11 @@
 			}
 			for (Class<?> clazz : extraComponents) {
 				descriptors.add(buildCondition(clazz));
+				addBeanMethodConditions(clazz, descriptors);
 			}
 			for (Class<?> clazz : componentClasses) {
 				descriptors.add(buildCondition(clazz));
+				addBeanMethodConditions(clazz, descriptors);
 			}
 			for (Class<?> clazz : configPropsClasses) {
 				descriptors.add(buildCondition(clazz));
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T16:59:03Z
Completed At: 2026-06-17T16:59:03Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -273,6 +273,22 @@
 							.sum());
 		}
 
+		private void addBeanMethodConditions(Class<?> clazz, List<summer.core.BeanCondition> descriptors) {
+			for (java.lang.reflect.Method m : clazz.getMethods()) {
+				if (m.isAnnotationPresent(summer.core.annotation.Bean.class)) {
+					Set<String> ifaces = new HashSet<>();
+					Class<?> retType = m.getReturnType();
+					if (retType.isInterface()) {
+						ifaces.add(retType.getName());
+					}
+					for (Class<?> iface : retType.getInterfaces()) {
+						ifaces.add(iface.getName());
+					}
+					descriptors.add(summer.core.BeanCondition.of(retType.getName(), ifaces, null, null));
+				}
+			}
+		}
+
 		private summer.core.BeanCondition buildCondition(Class<?> clazz) {
 			summer.core.annotation.ConditionalOnBean cond = clazz.getAnnotation(summer.core.annotation.ConditionalOnBean.class);
 			summer.core.annotation.Replaces repl = clazz.getAnnotation(summer.core.annotation.Replaces.class);
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-17T17:02:19Z
Completed At: 2026-06-17T17:02:33Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -231,38 +231,62 @@
 				log.debug("[Summer] Registered @ConfigurationProperties: {} (prefix={})", clazz.getSimpleName(), prefix);
 			}
 
-			List<Class<?>> order = topologicalSort(componentClasses, registry);
-
+			List<Class<?>> pendingComponents = new ArrayList<>(componentClasses);
 			Map<Class<?>, Object> configInstances = new LinkedHashMap<>();
-			for (Class<?> clazz : order) {
-				try {
-					if (!constructorDepsSatisfied(clazz, registry)) {
-						log.debug("[Summer] Skipping {} — constructor dependencies not met", clazz.getSimpleName());
-						continue;
-					}
-					Object instance = instantiate(clazz, registry);
-					registry.registerSingleton(clazz, instance);
-
-					if (configClassNames.contains(clazz.getName())) {
-						configInstances.put(clazz, instance);
-						log.debug("[Summer] Registered @Configuration: {}", clazz.getSimpleName());
-					} else {
-						for (Class<?> iface : clazz.getInterfaces()) {
-							if (registry.peek(iface) == null) {
-								registry.registerSingleton(iface, instance);
-							}
-						}
-						log.debug("[Summer] Registered component: {}", clazz.getSimpleName());
-					}
-				} catch (RuntimeException e) {
-					throw e;
-				} catch (Exception e) {
-					throw new RuntimeException("[Summer] Failed to create: " + clazz.getName(), e);
-				}
-			}
-
-			if (!configInstances.isEmpty()) {
-				BeanMethodResolver.resolve(configInstances, registry, toRemove);
+			List<summer.runtime.BeanMethodResolver.BeanMethod> pendingBeans = new ArrayList<>();
+
+			boolean changed;
+			do {
+				changed = false;
+				
+				// 1. Try to instantiate pending components
+				Iterator<Class<?>> it = pendingComponents.iterato
<truncated 244 bytes>
+							if (configClassNames.contains(clazz.getName())) {
+								configInstances.put(clazz, instance);
+								pendingBeans.addAll(summer.runtime.BeanMethodResolver.collectMethods(instance));
+								log.debug("[Summer] Registered @Configuration: {}", clazz.getSimpleName());
+							} else {
+								for (Class<?> iface : clazz.getInterfaces()) {
+									if (registry.peek(iface) == null) {
+										registry.registerSingleton(iface, instance);
+									}
+								}
+								log.debug("[Summer] Registered component: {}", clazz.getSimpleName());
+							}
+						} catch (RuntimeException e) {
+							throw e;
+						} catch (Exception e) {
+							throw new summer.core.exception.SummerException("Failed to instantiate " + clazz.getName(), e);
+						}
+						it.remove();
+						changed = true;
+					}
+				}
+
+				// 2. Try to instantiate pending @Bean methods
+				Iterator<summer.runtime.BeanMethodResolver.BeanMethod> bit = pendingBeans.iterator();
+				while (bit.hasNext()) {
+					summer.runtime.BeanMethodResolver.BeanMethod bm = bit.next();
+					if (summer.runtime.BeanMethodResolver.canResolve(bm, registry)) {
+						try {
+							summer.runtime.BeanMethodResolver.resolveMethod(bm, registry);
+						} catch (Exception e) {
+							throw new RuntimeException("[Summer] Failed to invoke @Bean: " + bm.method().getName(), e);
+						}
+						bit.remove();
+						changed = true;
+					}
+				}
+			} while (changed);
+
+			for (Class<?> clazz : pendingComponents) {
+				log.debug("[Summer] Skipping {} — constructor dependencies not met", clazz.getSimpleName());
 			}
 
 			log.info("[Summer] Discovered {} component(s), {} config(s), {} @Bean method(s)",
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.
==================
Created At: 2026-06-18T03:13:26Z
Completed At: 2026-06-18T03:13:26Z
The following changes were made by the replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -175,6 +175,9 @@
 
 			// Step 3: Extract metadata and resolve conditions BEFORE instantiation
 			List<summer.core.BeanCondition> descriptors = new ArrayList<>();
+			for (Class<?> clazz : registry.singletons().keySet()) {
+				descriptors.add(summer.core.BeanCondition.of(clazz.getName(), Set.of(), null, null));
+			}
 			for (Class<?> clazz : componentClasses) {
 				descriptors.add(buildCondition(clazz));
 			}
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-18T03:33:42Z
Completed At: 2026-06-18T03:33:56Z
The following changes were made by the multi_replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -215,42 +215,55 @@
 
 			List<Class<?>> order = topologicalSort(componentClasses, registry);
 
-			Map<Class<?>, Object> configInstances = new LinkedHashMap<>();
+			boolean changed = true;
+			while (changed) {
+				changed = false;
+				Map<Class<?>, Object> newConfigInstances = new LinkedHashMap<>();
+				
+				var it = order.iterator();
+				while (it.hasNext()) {
+					Class<?> clazz = it.next();
+					try {
+						if (constructorDepsSatisfied(clazz, registry)) {
+							Object instance = instantiate(clazz, registry);
+							registry.registerSingleton(clazz, instance);
+
+							if (configClassNames.contains(clazz.getName())) {
+								newConfigInstances.put(clazz, instance);
+								log.debug("[Summer] Registered @Configuration: {}", clazz.getSimpleName());
+							} else {
+								for (Class<?> iface : clazz.getInterfaces()) {
+									if (registry.peek(iface) == null) {
+										registry.registerSingleton(iface, instance);
+									}
+								}
+								log.debug("[Summer] Registered component: {}", clazz.getSimpleName());
+							}
+							it.remove();
+							changed = true;
+						}
+					} catch (RuntimeException e) {
+						throw e;
+					} catch (Exception e) {
+						throw new summer.core.exception.BeanCreationException(
+								summer.core.ErrorCode.BEAN_CREATION_FAILED,
+								"Failed to instantiate bean: " + clazz.getName(), e);
+					}
+				}
+
+				if (!newConfigInstances.isEmpty()) {
+					BeanMethodResolver.resolve(newConfigInstances, registry, toRemove);
+					changed = true;
+				}
+			}
+
 			for (Class<?> clazz : order) {
-				try {
-					if (!constructorDepsSatisfied(clazz, registry)) {
-						log.debug("[Summer] Skipping {} — constructor dependencies not met", clazz.getSimpleName());
-						continue;
-					}
-					Object instance = instantiate(clazz, registry);
-					registry.registerSingleton(clazz, instance);
-
-					if (configClassNames.contains(clazz.getName())) {
-						configInstances.put(clazz, instance);
-						log.debug("[Summer] Registered @Configuration: {}", clazz.getSimpleName());
-					} else {
-						for (Class<?> iface : clazz.getInterfaces()) {
-							if (registry.peek(iface) == null) {
-								registry.registerSingleton(iface, instance);
-							}
-						}
-						log.debug("[Summer] Registered component: {}", clazz.getSimpleName());
-					}
-				} catch (RuntimeException e) {
-					throw e;
-				} catch (Exception e) {
-					throw new RuntimeException("[Summer] Failed to create: " + clazz.getName(), e);
-				}
-			}
-
-			if (!configInstances.isEmpty()) {
-				BeanMethodResolver.resolve(configInstances, registry, toRemove);
-			}
+				log.debug("[Summer] Skipping {} - constructor dependencies not met", clazz.getSimpleName());
+			}
+
+			validateRegistry(registry);
 
 			log.info("[Summer] Discovered {} component(s), {} config(s), {} @Bean method(s)",
-					componentClasses.size() - configInstances.size(), configInstances.size(),
-					configInstances.values().stream()
-							.mapToInt(c -> (int) java.util.Arrays.stream(c.getClass().getDeclaredMethods())
 									.filter(m -> m.isAnnotationPresent(summer.core.annotation.Bean.class) && !toRemove.contains(m.getReturnType().getName())).count())
 							.sum());
 		}
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.
==================
Created At: 2026-06-18T03:34:03Z
Completed At: 2026-06-18T03:34:03Z
The following changes were made by the multi_replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -215,6 +215,7 @@
 
 			List<Class<?>> order = topologicalSort(componentClasses, registry);
 
+			Map<Class<?>, Object> configInstances = new LinkedHashMap<>();
 			boolean changed = true;
 			while (changed) {
 				changed = false;
@@ -229,6 +229,7 @@
 
 							if (configClassNames.contains(clazz.getName())) {
 								newConfigInstances.put(clazz, instance);
+								configInstances.put(clazz, instance);
 								log.debug("[Summer] Registered @Configuration: {}", clazz.getSimpleName());
 							} else {
 								for (Class<?> iface : clazz.getInterfaces()) {
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
==================
Created At: 2026-06-18T03:38:05Z
Completed At: 2026-06-18T03:38:16Z
The following changes were made by the multi_replace_file_content tool to: G:\codebase\Summer\summer-runtime\src\main\java\summer\runtime\RuntimeApplicationContext.java. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -161,6 +161,15 @@
 
 				try {
 					Class<?> clazz = Class.forName(className);
+					for (Class<?> extra : extraComponents) {
+						componentClasses.add(extra);
+						if (extra.isAnnotationPresent(Configuration.class)) {
+							configClassNames.add(extra.getName());
+						}
+						if (extra.isAnnotationPresent(ConfigurationProperties.class)) {
+							configPropsClasses.add(extra);
+						}
+					}
 					if (clazz.isInterface() || clazz.isAnnotation() || clazz.isEnum())
 						continue;
 					if (configPropsClassNames.contains(className)) {
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.

We did our best to apply changes despite some inaccuracies. Double check if the edit applied is what you intended.
==================
