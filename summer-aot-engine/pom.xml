<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.dropguard</groupId>
        <artifactId>summer-framework</artifactId>
        <version>0.1.1-SNAPSHOT</version>
    </parent>

    <artifactId>summer-aot-engine</artifactId>
    <name>Summer AOT Engine</name>
    <description>Compile-time DI engine: bean discovery, dependency resolution, and AOT code generation</description>

    <dependencies>
        <!-- Jandex for class metadata indexing -->
        <dependency>
            <groupId>io.smallrye</groupId>
            <artifactId>jandex</artifactId>
        </dependency>

        <!-- JavaPoet for source code generation -->
        <dependency>
            <groupId>com.palantir.javapoet</groupId>
            <artifactId>javapoet</artifactId>
        </dependency>

        <!-- Summer Core (annotations, BeanContainer, exceptions, reflection-free config binding) -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Summer Engine (shared discovery pipeline + ContainerEngine SPI) -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-engine</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Summer AOP (for interceptor analysis) -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-aop</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Shared test fixtures (generic DI/Web dummy beans used by
             CrossModuleDiscoveryTest). Keeps fixture classes in one place
             instead of duplicated inline in this module's test tree. -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-tck-fixtures</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-tck-negative-fixtures</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Runtime web bridge (test scope): its WebRouteScanner is the ServiceLoader-discovered
             RouteRegistrar implementation the SPI collector needs on the test classpath -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-runtime-web</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- @SummerTest/@DualEngine test infrastructure (brings the runtime engine for the RUNTIME leg) -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-test</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Data JDBC (REAL type dependency: WireMethodGenerator / TypeReads call
             RowMapperFactory.scanJandex/camelToSnake/resolveFieldType and RowModelMeta by
             fully-qualified name — the AOT generator needs the JDBC type-mapping contract.
             This is NOT redundant classpath supply like the removed summer-web dep. -->
        <dependency>
            <groupId>com.github.dropguard</groupId>
            <artifactId>summer-data-jdbc</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              JaCoCo: instruments this module's own tests (e.g.
              CrossModuleDiscoveryTest) so the hand-written AOT engine
              (com.github.dropguard.summer.aot.*) is measured directly, not only transitively
              through the TCK. prepare-agent is inherited from pluginManagement.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
