<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>summer</groupId>
    <artifactId>summer-framework</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>

    <name>Summer Framework</name>
    <description>A minimal, chain-oriented micro framework for building CRUD APIs on modern JDK</description>
    <url>https://github.com/your-username/summer</url>

    <properties>
        <java.version>26</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jacoco.version>0.8.15</jacoco.version>
        <!-- Default empty — JaCoCo prepare-agent overrides when active -->
        <argLine></argLine>
    </properties>

    <modules>
        <module>summer-core</module>
        <module>summer-web</module>
        <module>summer-web-http</module>
        <module>summer-web-netty</module>
        <module>summer-web-middleware</module>
        <module>summer-web-websocket</module>
        <module>summer-boot</module>
        <module>summer-aop</module>
        <module>summer-tx</module>
        <module>summer-runtime</module>
        <module>summer-dependencies</module>
        <module>summer-starter-parent</module>
        <module>summer-grpc</module>
        <module>summer-data-redis</module>
        <module>summer-data-jdbc</module>
        <module>summer-test</module>
        <module>summer-example</module>
        <module>summer-realworld</module>
        <module>summer-tck</module>
        <module>summer-maven-plugin</module>
        <module>summer-tck-fixtures</module>
        <module>summer-archunit</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Import Summer BOM for all dependency versions -->
            <dependency>
                <groupId>summer</groupId>
                <artifactId>summer-dependencies</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.15.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.5</version>
                    <configuration>
                        <argLine>@{argLine} --sun-misc-unsafe-memory-access=allow</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.5.5</version>
                    <configuration>
                        <argLine>@{argLine} --sun-misc-unsafe-memory-access=allow</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>io.smallrye</groupId>
                    <artifactId>jandex-maven-plugin</artifactId>
                    <version>3.5.3</version>
                    <executions>
                        <execution>
                            <id>make-index</id>
                            <goals>
                                <goal>jandex</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!-- Add Spotless for static analysis and formatting -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>2.44.5</version>
                    <configuration>
                        <java>
                            <!-- Eclipse JDT Formatter (independent of javac internals, works on JDK 25) -->
                            <eclipse>
                                <version>4.26</version>
                            </eclipse>
                            <!-- Automatically remove all unused imports -->
                            <removeUnusedImports />
                            <!-- Enforce consistent import ordering -->
                            <importOrder />
                        </java>
                    </configuration>
                </plugin>
                <!-- JaCoCo for code coverage — opt-in: add to <plugins> in modules that need it -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>test</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!--
                Clean annotation-processor-generated sources before every build.
                Prevents stale .class pollution from incremental compilation:
                if AP-generated code failed to compile in a previous run, the
                poisoned .class files persist in target/classes/ and javac's
                incremental mode skips recompiling them.

                This execution runs at the initialize phase (before compile)
                and only touches generated-sources directories — main sources
                and compiled classes are untouched.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>clean-generated-sources</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                        <configuration>
                            <excludeDefaultDirectories>true</excludeDefaultDirectories>
                            <filesets>
                                <fileset>
                                    <!--
                                        AP-generated sources (avaje-validator-generator).
                                        SummerMojo cleans its own generated-sources/aot/ before
                                        generating — no external cleanup needed for AOT.
                                    -->
                                    <directory>${project.build.directory}/generated-sources/annotations</directory>
                                </fileset>
                            </filesets>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <id>maven-releases</id>
            <name>Maven Releases</name>
            <url>https://your-repository/releases</url>
        </repository>
        <snapshotRepository>
            <id>maven-snapshots</id>
            <name>Maven Snapshots</name>
            <url>https://your-repository/snapshots</url>
        </snapshotRepository>
    </distributionManagement>
</project>